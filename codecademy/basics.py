# days = [13, 28, 56, 79]
# doubled = [n * 2 for n in days]
# print(doubled)

temperature = [2, -1, 79, 33, -45]
only_negative = [tem * 2 for tem in temperature if tem < 0]

# for num in numbers:
#   if num < 0: 
#     only_negative_doubled.append(num * 2)
print(only_negative) 